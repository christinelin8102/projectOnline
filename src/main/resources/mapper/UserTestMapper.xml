<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.christine.mapper.UserTestMapper">
    <select id="findByUserNo" resultType="com.myself.mybatis.entity.TMsg">
        SELECT user_name,age from user_test WHERE user_no = #{user_no}
    </select>
</mapper>